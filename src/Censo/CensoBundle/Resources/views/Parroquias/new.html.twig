{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Parroquias </h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('parroquias') }}">
            Volver a la Lista
        </a>
    </li>
</ul>
{% endblock %}
