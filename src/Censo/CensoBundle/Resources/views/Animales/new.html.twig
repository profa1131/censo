{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Animales </h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('animales') }}">
            Volver a la Lista
        </a>
    </li>
</ul>
{% endblock %}
