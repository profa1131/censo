{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Profesiones editar</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('profesiones') }}">
            Volver a la Lista
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
