{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Groups </h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('groups') }}">
            Volver a la Lista
        </a>
    </li>
</ul>
{% endblock %}
