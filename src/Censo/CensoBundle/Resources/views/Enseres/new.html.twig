{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Enseres </h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('enseres') }}">
            Volver a la Lista
        </a>
    </li>
</ul>
{% endblock %}
