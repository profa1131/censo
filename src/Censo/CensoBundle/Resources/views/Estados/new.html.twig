{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Estados </h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('estados') }}">
            Volver a la Lista
        </a>
    </li>
</ul>
{% endblock %}
