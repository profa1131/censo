{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Preguntas </h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('preguntas') }}">
            Volver a la Lista
        </a>
    </li>
</ul>
{% endblock %}
